buildscript {
    ext.kotlin_version = '1.2.10'

    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

group 'com.github.alessandroscarlatti'
version '0.1.0-SNAPSHOT'

apply plugin: 'java'
apply plugin: 'kotlin'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile "org.jetbrains.kotlin:kotlin-stdlib-jdk8:$kotlin_version"
    compile "ch.qos.logback:logback-core:1.2.3"
    compile "org.slf4j:slf4j-api:1.7.28"
    compile "ch.qos.logback:logback-classic:1.2.3"
    compile group: 'org.apache.commons', name: 'commons-exec', version: '1.3'
    compile group: 'commons-io', name: 'commons-io', version: '2.6'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

compileKotlin {
    kotlinOptions.jvmTarget = "1.8"
}
compileTestKotlin {
    kotlinOptions.jvmTarget = "1.8"
}

jar {
    archiveName = "ContextMenuHelper.jar"
    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
    manifest {
        attributes([
                "Main-Class" : "io.github.alessandroscarlatti.WindowsContextMenuHelper"
        ])
    }
}

task buildDist {
    doLast {
        // assemble the example dist
        // start from an empty dist dir
        mkdir(file("dist"))

        copy {
            from file("Project")
            from file("build/libs/ContextMenuHelper.jar")
            into file("dist")
        }
    }
}

// build the dist after building the jar
build.finalizedBy(buildDist)